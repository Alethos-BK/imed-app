import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  iconContainer: {
    position: "absolute",
    top: 10,
    right: 10,
    padding: 20,
  },
  favoriteIcon: {
    position: "absolute",
    top: 10,
    right: 10,
  }
})